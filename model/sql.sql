CREATE TABLE person (
    id    INTEGER   PRIMARY KEY AUTOINCREMENT,
    name  CHAR (50),
    email CHAR (50) 
);

CREATE TABLE product (
    id      INTEGER   PRIMARY KEY AUTOINCREMENT,
    name    CHAR (50),
    product DOUBLE
);
